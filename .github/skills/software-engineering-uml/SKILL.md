---
name: software-engineering-uml
description: '根据现有项目源码分析系统结构，推荐并生成标准 PlantUML 图。适用于 Java、Python、C、C++、JavaScript、TypeScript、SQL 等项目的用例图、类图、时序图、活动图、状态图和数据库 ER 图；同时检查源码错误、冲突、缺失与最小可执行原型风险。'
argument-hint: '[项目路径或源码范围；可补充业务场景、目标用户或关注的 UML 图]'
user-invocable: true
disable-model-invocation: false
---

# 软件工程 UML 生成器

## 核心职责

从已有项目源码中提取可验证的结构和行为事实，结合用户的自然语言目标，推荐当前最适合的 UML 图，并生成可直接使用的 PlantUML 源文件。分析只读源码和配置，不修改被分析项目。

除了画图，还要检查最小可执行原型（MVP）是否成立：识别编译/语法风险、模块冲突、缺失依赖、未实现接口、数据模型不一致、关键流程断裂和无法由源码证实的关系。所有问题在最终反馈中集中列出，并区分事实、推断和待确认项。

## 支持范围

- UML：用例图、类图、时序图、活动图、状态图。
- 数据建模：数据库领域 ER 图；从 SQL DDL、ORM 模型和数据库访问代码中提取实体、字段、主键、外键和关系。
- 语言：Java、Python、C、C++、JavaScript、TypeScript、SQL；遇到其他语言时仍可扫描通用结构，但必须标明解析置信度。
- 输出：PlantUML `.puml` 文件，可附带 `analysis-report.md` 和机器可读的 `analysis.json`。

## 使用流程

### 1. 明确范围和目标

1. 确认项目根目录、源码范围、排除目录和用户关心的业务场景。
2. 先读取项目说明、构建文件、入口文件、路由/控制器、领域模型、数据库 DDL 和测试文件。
3. 默认排除 `.git`、`.idea`、`.vscode`、`node_modules`、`target`、`build`、`dist`、`__pycache__`、`.venv`、`venv`、`bin`、`obj` 和生成文件；除非用户明确要求分析它们。
4. 不修改源码，不运行会改变项目数据的命令；优先使用静态分析和只读检查。

### 2. 运行源码事实扫描

在 Skill 目录下执行：

```powershell
python .github/skills/software-engineering-uml/scripts/analyze_project.py <project-root> --output <output-dir>
```

若当前目录就是项目根目录：

```powershell
python .github/skills/software-engineering-uml/scripts/analyze_project.py . --output .uml-analysis
```

脚本输出 `analysis.json`，包含文件、语言、类/接口/结构体、函数/方法、导入/包含、继承/实现、SQL 表与约束、入口候选以及扫描告警。脚本是证据收集器，不替代完整编译器、IDE 或运行时测试。

### 3. 推荐最合适的图

根据用户问题和扫描事实推荐 1 个主图，必要时列出 1-2 个补充图，解释“为什么这个图最能回答当前问题”。遵循以下判断：

- 用户、角色和系统边界不清楚：用例图。
- 类、接口、继承、组合和依赖是重点：类图。
- 需要解释一次请求如何跨模块调用：时序图。
- 需要表达业务步骤、分支、并行或异常路径：活动图。
- 需要表达对象生命周期、状态转移和事件：状态图。
- 需要展示表、字段、主外键和基数：数据库 ER 图。

不要为了“图越多越好”而生成无关图。若证据不足，推荐图仍可生成，但必须在报告中写明缺失证据和置信度。

### 4. 生成 PlantUML

1. 每张图单独保存为 `<类型>-<主题>.puml`，文件名使用小写英文、数字和连字符。
2. 使用 `@startuml` / `@enduml`，必要时加入 `title`、`legend` 和 `hide` 语句。
3. 关系必须尽量对应扫描结果；不能从源码确认的关系使用注释或 `<<inferred>>` 标注，不得伪装成确定事实。
4. 类图保留关键属性、方法、可见性、继承、实现和关联；避免把所有私有细节无差别塞进一张图。
5. 时序图选择一个明确场景，包含参与者、入口、关键调用、返回、异常和外部依赖。
6. 活动图包含开始、结束、关键动作、条件分支和异常/失败路径。
7. 状态图必须有明确对象、状态、触发事件和转移条件。
8. ER 图使用实体、主键、外键、字段类型和基数；SQL 中缺失约束时不要臆造关系。
9. PlantUML 源文件必须包含必要的中文文本转义或直接使用 UTF-8；优先保持图中文字简洁。

### 5. MVP 质量检查

按优先级检查并记录：

- **阻断项**：明显语法错误、缺失入口、无法解析的关键文件、缺失核心依赖、循环依赖导致的启动风险。
- **高风险项**：接口与实现不匹配、调用了不存在的方法/字段、SQL 表列与代码不一致、主外键类型冲突、关键异常路径缺失。
- **改进项**：测试缺失、职责过重、重复模型、命名不一致、未使用结构、文档与源码不一致。
- **待确认项**：仅凭静态源码无法判断的运行时配置、反射/动态导入、外部服务契约和业务规则。

每条问题包含：严重级别、文件位置、证据、影响、建议动作。不要把“可能存在”写成确定错误；明确区分 `fact`、`inference` 和 `unknown`。

### 6. 最终反馈格式

最终输出按以下顺序：

1. 分析范围和扫描统计。
2. 推荐的主 UML 图、补充图及推荐理由。
3. 生成文件清单及每张图的用途。
4. MVP 问题清单，按阻断项、高风险项、改进项、待确认项排序。
5. 未覆盖范围、解析限制和下一步建议。

若用户只要求推荐而没有要求落盘，可以先展示推荐和计划；若用户要求执行，则生成 `.puml` 和报告文件。任何自动修复都必须另行征得用户同意。

## 资源

- [源码事实扫描脚本](./scripts/analyze_project.py)
- [PlantUML 建模约定](./references/plantuml-patterns.md)
